/** Layout padrão das barras de filtro (listas/tabelas). */
export const filterToolbarClassName =
  'flex flex-col gap-3 xl:flex-row xl:flex-wrap xl:items-center';

/** Campo de busca — cresce na linha e mantém altura dos demais controles. */
export const filterSearchFieldClassName = 'relative w-full xl:min-w-[200px] xl:flex-1';

/** Select médio (bolão, campeonato). */
export const filterSelectMdClassName = 'w-full xl:w-56';

/** Select largo (regra de pontuação). */
export const filterSelectLgClassName = 'w-full xl:w-64';

/** Select compacto (status). */
export const filterSelectSmClassName = 'w-full xl:w-44';

/** Input de data. */
export const filterDateInputClassName = 'w-full xl:w-40';
