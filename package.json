{
  "name": "soccer-predictions",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm install --prefix apps/api --include=dev && npm install --prefix apps/web --include=dev && node scripts/fix-prisma-binaries.mjs",
    "build": "npm run build --prefix apps/api && npm run build --prefix apps/web",
    "build:deploy": "npm run build",
    "db:migrate": "npm run prisma:migrate:deploy --prefix apps/api",
    "db:sql": "node scripts/export-migrations-sql.mjs",
    "start": "node apps/web/server.mjs",
    "start:prod": "node apps/web/server.mjs",
    "start:unified": "node scripts/start-production.mjs"
  }
}
