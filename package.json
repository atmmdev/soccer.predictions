{
  "name": "soccer-predictions",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm install --prefix apps/api --include=dev && npm install --prefix apps/web --include=dev",
    "build": "npm run build --prefix apps/api && npm run build --prefix apps/web",
    "build:deploy": "npm run build",
    "start": "node scripts/hostinger-server.mjs",
    "start:prod": "node scripts/hostinger-server.mjs",
    "start:unified": "node scripts/start-production.mjs"
  }
}
